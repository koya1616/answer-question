= form_with model: Like.find_by(user: current_user, likeable: likeable), remote: true, method: :delete do |f|
  = button_tag type: :submit, class: "unlike_#{likeable.id}" do
    i.fas.fa-heart.text-danger.like_btn
